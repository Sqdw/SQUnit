This is a library that provides simple access to QUnit tests from NUnit using Selenium WebDriver.

It is vastly based on the <a href = "https://github.com/robdmoore/NQUnit">NQUnit</a>, which uses WatiN to do the same.